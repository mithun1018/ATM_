package ATM_Console_Application.Notes;

public class FiveHundred extends Notes{
    public FiveHundred(String notes,int count){
        this.setNotes(notes);
        this.setCount(count);
    }
}
